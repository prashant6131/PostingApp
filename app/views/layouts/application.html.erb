<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Demopost App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://kit.fontawesome.com/47f995c876.js" crossorigin="anonymous"></script>

  </head>

  <body>
    <div class = "container">
      <h1>Posting</h1>
      <header>
        <nav>
          <div class="header-container d-flex justify-content-end align-items-center">
          <% if user_signed_in? %>
            <div class="d-flex align-items-center">
              <%= button_to 'Edit profile', edit_user_registration_path, method: :get, class: 'btn btn-info mx-2 edit-profile-button' %>
              <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-danger mx-2 signout-button' %>
            </div>
          <% else %>
            <div class="d-flex align-items-center">
              <%= button_to 'Log In', new_user_session_path, method: :get, class: 'btn btn-primary mx-2' %>
              <%= button_to 'Sign Up', new_user_registration_path, method: :get, class: 'btn btn-success mx-2' %>
            </div>
          <% end %>

          
            <div class="current_user ms-3">
              <% if user_signed_in? %>
                <%= current_user.email %>
              <% end %>
            </div>
        </div>
        
        </nav>
      </header>
    </div>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
