<%= form_with(model: [post, Comment.new], local: false) do |form| %>
    <div class="form-group">
        <%= form.text_area :description, placeholder: "Add a comment...", rows: 3, class: "form-control" %>
    </div>
    <%= form.submit "post", class: 'btn btn-primary' %>
<% end %>
